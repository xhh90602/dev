export enum IM_STATUS_ENUM {
  open,
}

export enum MSG_TYPE {
  TEXT = 'txt',
  UPPERTEXT = 'TEXT',
  IMAGE = 'img',
}

export enum SUPPORT_IMG_TYPE {
  gif = 1,
  bmp,
  jpg,
  png,
  jpeg,
}

export enum CHAT_TYPE {
  chatRoom = 'groupchat',
  singleChat = 'chat',
}

export enum ALERT_LIST_ACTIONS {
  replace,
  push,
  unshift,
}

export enum ALERT_TYPE {
  error,
}
